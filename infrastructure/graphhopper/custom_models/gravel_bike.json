{
  "priority": [
    {"if": "road_class == STEPS || road_class == FOOTWAY || road_class == PEDESTRIAN", "multiply_by": "0.05"},
    {"if": "road_class == MOTORWAY || road_class == TRUNK || road_class == PRIMARY", "multiply_by": "0.20"},
    {"if": "smoothness == VERY_BAD || smoothness == HORRIBLE || smoothness == VERY_HORRIBLE || smoothness == IMPASSABLE", "multiply_by": "0.05"},
    {"if": "mtb_rating > 3", "multiply_by": "0.10"},
    {"if": "surface == SAND", "multiply_by": "0.30"},
    {"if": "surface == ASPHALT || surface == CONCRETE", "multiply_by": "0.85"}
  ],
  "distance_influence": 70
}
